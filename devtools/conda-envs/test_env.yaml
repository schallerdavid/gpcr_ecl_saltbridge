name: gpcr_ecl_saltbridge
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Other
  - biopython
  - jupyterlab

    # Pip-only installs
  #- pip:
  #  - codecov

